冒泡排序：
#include<stdio.h>
void maopao(int* arr, int sz)
{
	int i, j;
	for (int i = 0;i < sz - 1;i++)
	{
		for (int j = 0;j < sz - i - 1;j++)
		{
			if (arr[j] > arr[j + 1])
			{
				int temp = arr[j];
				arr[j] = arr[j + 1];
				arr[j + 1] = temp;
			}
		}
	}
}
int main()
{
	int arr[] = { 2,6,4,6,98,5,0,58,5, };
	int sz = sizeof(arr) / sizeof(arr[0]);
	maopao(arr, sz);
	for (int i = 0;i < sz;i++)
	{
		printf("%d ", arr[i]);
	}
}
写一个函数，判断一个字符串是否为另外一个字符串旋转之后的字符串。
例如：给定s1 =AABCD和s2 = BCDAA，返回1
给定s1=abcd和s2=ACBD，返回0.
AABCD左旋一个字符得到ABCDA
#include<stdio.h>
#include<string.h>
int Function(char* str1, char* str2,int len)
{
	
	for (int i = 0;i < len;i++)
	{
		char tmp = str1[0];
		for (int j = 0;j < len;j++)
		{
			str1[j] = str1[j + 1];
		}
		str1[len - 1] = tmp;
		if (strcmp(str1, str2) == 0)
		{
			return 1;
		}
	}
	return 0;
}
int main()
{
	char str1[] = "asdfgh";
	char str2[] = "sdfgha";
	int len = strlen(str1);
	int ret=Function(str1, str2,len);
	printf("%d",ret);
}
日本某地发生了一件谋杀案，警察通过排查确定杀人凶手必为4个嫌疑犯的一个。
以下为4个嫌疑犯的供词:
A说：不是我。
B说：是C。
C说：是D。
D说：C在胡说
已知3个人说了真话，1个人说的是假话
#include<stdio.h>
int main() 
{
	for (int i = 'a';i <= 'd';i++)
	{
		if ((i != 'a') + (i == 'c') + (i == 'd') + (i != 'd') == 3)
		{
			printf("%c ", i);
		}
	}
}
杨氏矩阵
作业内容
有一个数字矩阵，矩阵的每行从左到右是递增的，矩阵从上到下是递增的，请编写程序在这样的矩阵中查找某个数字是否存在。
要求：时间复杂度小于O(N);
杨氏矩阵
#include<stdio.h>
int get_key(int* arr[][3], int x, int y, int n)
{
	int i = 0;
	int j = y - 1;
	while (i < x && j >= 0)
	{
		if (arr[i][j] < n)
		{
			i++;
		}
		else if (arr[i][j] > n)
		{
			j--;
		}
		else return 1;
	}
	return 0;
}
int main()
{
	int arr[3][3] = { {1,2,3},{4,5,6},{7,8,9} };
	int key = 5;
	printf("%d", get_key(arr, 3, 3, key));
	return 0;
}
在屏幕上打印杨辉三角。#include<stdio.h>
int main()
{
	int n;
	scanf("%d", &n);
	int arr[100][100];
	for (int i = 0;i < n;i++)
	{
		for (int j = 0;j <= i;j++)
		{
			if (j == 0 || j == i)
			{
				arr[i][j] = 1;
			}
			else
			{
				arr[i][j] = arr[i - 1][j] + arr[i - 1][j - 1];
			}
		}
	}
	for (int i = 0;i < n;i++)
	{
		for (int j = 0;j <= i;j++)
		{
			printf("%2d ", arr[i][j]);
		}
		printf("\n");
	}
	return 0;
}
