求10 个整数中最大值

#include<stdio.h>
int main()
{
	int i, max;
	max = 0;
	int arr[10];
	for (i = 0;i < 10;i++)
	{
		scanf_s("%d", &arr[i]);
	}
	for (i = 0;i < 10;i++)
	{
		if (max < arr[i])
			max = arr[i];
	}
	printf("%d", max);
}
